// Generated by insbind. Conventions mirror the hand-written
// windows::*/unix::* bindings: opaque handles and function
// pointers are u64, C strings are text (const) or u8*
// (mutable), constants are zero-argument functions.

module m1

export type uLong = u32

export type comparator = func<(void*, void*) -> i32>

export extern fun [dll("m1.dll")] add(i32 a, i32 b) -> i32

export extern fun [dll("m1.dll")] name_of(i32 id) -> text

export extern fun [dll("m1.dll")] buffer_of(i32 slot) -> u8*

export extern fun [dll("m1.dll")] opaque_handle() -> void*

export extern fun [dll("m1.dll")] compressBound(u32 sourceLen) -> u32

export extern fun [dll("m1.dll")] sort_with(void* base, u32 count, u32 size, func<(void*, void*) -> i32> cmp) -> void

// skipped variadic function (needs a C shim): printf_like

// skipped static inline function: max2

// extern global (needs an accessor shim): errno_like_global

!warn extern global skipped: errno_like_global
