enum Color { Red, Green, Blue };
enum Explicit { Off = 0, On = 1, Standby = 10, Sleep };
enum Negative { ErrA = -3, ErrB = -2, Ok = 0 };
enum Trailing { One = 1, Two = 2, };
enum Big { Huge = 0xffffffff, Bigger = 0x100000000 };
enum Color favorite;
typedef enum { Fast, Slow } Speed;
