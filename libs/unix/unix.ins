// Insty standard library: unix
//
// Placeholder for shared Unix (non-Windows) bindings used by both the
// linux and macos backends: raw syscalls, errno, file descriptors, and
// other POSIX-adjacent primitives that are common across Unix-like
// systems. The higher-level `std` modules will layer a portable API on
// top of these.
//
// Intended import paths once populated, e.g.:
//     import unix::io
//     import unix::process
//
// Empty for now; this file reserves the `unix` module namespace and keeps
// the directory under version control and in the copied stdlib tree.

module unix
