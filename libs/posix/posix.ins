// Insty standard library: posix
//
// Placeholder for POSIX bindings that are *not* Linux- or macOS-specific:
// portable POSIX APIs (signals, termios, pthreads-style primitives, etc.)
// shared by Unix-like targets that are neither plain Linux nor Darwin.
//
// Intended import paths once populated, e.g.:
//     import posix::signal
//     import posix::time
//
// Empty for now; this file reserves the `posix` module namespace and keeps
// the directory under version control and in the copied stdlib tree.

module posix
